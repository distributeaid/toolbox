.always: &always
  except:
    refs:
      - tags
    variables:
      - $PIPELINE_NAME == "dispatcher"

.master: &master
  only:
    refs:
      - master
  extends: .always

.elixir:
  only:
    variables:
      - $PIPELINE_NAME =~ /lib/
  variables:
    PROJECT_NAME: lib

.cleanup:
  only:
    variables:
      - $PIPELINE_NAME == "cleanup"
