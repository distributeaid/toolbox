query getChapterBySlug($slug: String!) {
  groupBySlug(slug: $slug) {
    id
    name
    slug
    description
    donationForm
    donationLink
    donationFormResults
    location {
      countryCode
      province
      postalCode
    }
    slackChannelName
    volunteerForm
    volunteerFormResults
    requestForm
    requestFormResults
  }
}
